#!/usr/bin/env bash
# Usage: ./script/test/alpine <image-ref>
set -euo pipefail

image="${1:?usage: $0 <image-ref>}"
dir="$(cd "$(dirname "$0")" && pwd)"

exec container-structure-test test \
  --image "$image" \
  --config "$dir/common.yaml" \
  --config "$dir/alpine.yaml"
